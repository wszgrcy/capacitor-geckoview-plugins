import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitorjs.camera.sample',
  appName: 'CAP-CAMR-SAMPLE',
  webDir: 'dist'
};

export default config;
